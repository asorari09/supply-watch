# Open-Meteo fixtures

`normal-houston.json` is a captured Open-Meteo forecast response from 2026-07-13.
`storm-houston-beryl.json` is a captured Open-Meteo archive response for Hurricane Beryl on 2024-07-08.
The malformed files are synthetic. All adapter tests use only these deterministic fixtures and never call a live API.
